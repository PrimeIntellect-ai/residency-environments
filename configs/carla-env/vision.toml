num_tasks = 19
num_rollouts = 2
max_concurrent = 1
push = false

[env.taskset]
id = "carla-env"
modality = "vision"
families = ["decision", "maze", "navigation", "free_roam"]
seeds = [0, 1]

[env.taskset.task.tools]
colocated = false

[env.taskset.task.tools.runtime]
type = "docker"
image = "sinatras/carla-env-runtime@sha256:6e85ee2ce482bc3801a656784f328152a1777e28e12b52b155de28864021fe63"
workdir = "/home/carla"
cpu = 4
memory = 8
gpu = "1"

[env.agent.harness]
id = "null"

[env.agent.runtime]
type = "prime"
vm = true

# Optional safeguard: an expired rollout timeout fails the rollout without a score.
# To end episodes on time, set max_sim_seconds or max_wall_seconds in
# env.taskset.env_args instead.
[env.agent.timeout]
setup = 600
rollout = 600
finalize = 120
