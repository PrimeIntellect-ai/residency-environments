# configs

Reusable configuration files for testing and evaluating environments.

Keep environment-specific configurations in `configs/<env-name>/`. Configurations should be portable, contain no credentials or other secrets, and describe the settings needed to reproduce the intended test or evaluation. Evaluation outputs do not belong here.
