[build-system]
requires = ["uv_build>=0.9.26,<0.13.0"]
build-backend = "uv_build"

[project]
name = "alphaverse-clock-probe"
version = "0.0.1"
requires-python = "~=3.12.0"
description = "No-inference grouped eval probe for deterministic market time"
